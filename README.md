this is a scratch implemented cnn with 10 filters and 10 pooling layers that was trained on fashion images with data extracted from kaggle.
The cnn gives an accuracy of 76% being able to generalise the patterns from the image well, later the model is to be wrapped around a flask api.
